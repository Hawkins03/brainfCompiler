{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "parser (choose file)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/parser",
            "args": ["${input:compileFile}"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask": "build (make)"
        },
        {
            "name": "semChecker (choose file)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/semChecker",
            "args": ["${input:compileFile}"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask": "build (make)"
        }
    ]
    ,
    "inputs": [
        {
            "id": "compileFile",
            "type": "promptString",
            "description": "Path to the source file to compile/run (relative to workspace)",
            "default": "${file}"
        }
    ]
}

